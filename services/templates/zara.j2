    # service: zara
    - name: unarchive zara
      unarchive:
        src: services/zara/zara.tar.gz
        remote_src: no
        copy: yes
        dest: /opt/
 {# - name: copy all files in place
      become: no
      become_user: root
      copy:
        src: {{ path }} # zara.tar.gz location
        dest: /home/vagrant/zara
        owner: vagrant
        group: vagrant
       mode: '0644'

    - name: decompression
      script: /home/{{ USER }}/zara/script.sh
 #}